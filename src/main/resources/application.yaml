spring:
  application:
    name: patrol

server:
  port: 8080

patrol:
  cors:
    # Origins of the delivered frontend. Override with PATROL_CORS_ALLOWED_ORIGINS.
    allowed-origins: http://localhost:5173,http://localhost:3000,http://localhost:4200,http://127.0.0.1:5500

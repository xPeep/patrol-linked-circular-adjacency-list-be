# A broken circular structure typically means an endless loop, not a wrong value.
# A default timeout turns such a hang into a readable failure instead of a frozen build.
junit.jupiter.execution.timeout.default = 60 s
junit.jupiter.execution.timeout.thread.mode.default = separate_thread
